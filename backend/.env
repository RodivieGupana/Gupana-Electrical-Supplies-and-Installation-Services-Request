# PostgreSQL connection
DATABASE_URL=postgresql://postgres:admin123@localhost:5432/gupana_db

# Or individual params (used if DATABASE_URL is not set)
PGHOST=localhost
PGPORT=5432
PGDATABASE=gupana_db
PGUSER=postgres
PGPASSWORD=admin123

# App
PORT=4000
JWT_SECRET=Gupana2026@SecureJWT#System123g
JWT_EXPIRES_IN=7d

# CORS - the origin(s) your frontend is served from
CLIENT_ORIGIN=http://127.0.0.1:5500
